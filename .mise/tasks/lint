#!/usr/bin/env bash
# mise task: lint
# Run linters only
set -euo pipefail

echo "=== cargo fmt (check) ==="
cargo fmt --check

echo "=== cargo clippy ==="
cargo clippy --all-targets --all-features

echo "=== cargo deny check ==="
cargo deny check 2>&1 || echo "[warn] cargo-deny not installed"
