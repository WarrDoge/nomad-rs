#!/usr/bin/env nu
# mise task: lint
# Run linters only

print "=== cargo fmt (check) ==="
cargo fmt --check

print "=== cargo clippy ==="
cargo clippy --all-targets --all-features

print "=== cargo deny check ==="
try { cargo deny check } catch { print "[warn] cargo-deny not installed" }
